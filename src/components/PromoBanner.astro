---
import { Tag, Ticket } from 'lucide-astro'

const wa =
  'https://wa.me/56954161984?text=' +
  encodeURIComponent(
    'Hola! Me interesan los productos y tengo mi cupón de Navidad 🎄 #CUPONNAVIDAD'
  )
---

<div
  class="bg-gradient-to-r from-white to-teal-50 border-b border-teal-200
            py-4 px-4 shadow-sm z-[60] ring-1 ring-teal-100"
>
  <div
    class="max-w-7xl mx-auto flex flex-col sm:flex-row items-center gap-3 text-center sm:text-left"
  >
    <!-- Descuento destacado -->
    <div class="flex items-center gap-2 text-lg font-extrabold text-teal-900">
      <Tag class="w-5 h-5 text-teal-700" />
      <span>🎄 20% en Estanques</span>
    </div>

    <!-- Divider -->
    <div class="hidden sm:block h-6 w-px bg-teal-300"></div>

    <!-- Cupón + CTA -->
    <div class="flex items-center gap-3">
      <div class="flex items-center gap-1 text-sm font-medium text-teal-800">
        <Ticket class="w-4 h-4 text-teal-700" />
        <span>Cupón: <strong>#CUPONNAVIDAD</strong></span>
      </div>

      <a
        href={wa}
        target="_blank"
        class="flex items-center gap-2 px-3 py-1.5 text-xs font-bold
               bg-teal-700 text-white rounded-lg hover:bg-teal-800
               active:scale-95 transition-all shadow-md"
      >
        Enviar cupon →
      </a>
    </div>

    <!-- Divider -->
    <div class="hidden sm:block h-6 w-px bg-teal-300"></div>

    <!-- Subtexto animado -->
    <div
      class="text-xs sm:text-sm text-teal-700 sm:ml-auto opacity-95
                motion-safe:animate-fadeInUp"
    >
      Hasta el 31 de diciembre · Valídalo por WhatsApp
    </div>
  </div>
</div>

<style>
  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(6px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
  .animate-fadeInUp {
    animation: fadeInUp 0.6s ease-out forwards;
  }
</style>
